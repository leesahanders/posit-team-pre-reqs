The Prompt: You can now simply say: "Based on the documentation, write a bash script that a customer can run to check if their RHEL 9 server is ready for a Workbench and Connect install."

Make sure the execution script has the right permissions: `chmod +x posit-ready.sh` 

Run with: `sudo ./posit-ready.sh`